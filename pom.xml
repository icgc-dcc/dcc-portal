<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.icgc.dcc</groupId>
  <artifactId>dcc-parent</artifactId>
  <packaging>pom</packaging>
  <version>1.5</version>
  <modules>
    <module>server</module>
    <module>client</module>
  </modules>
  <scm>
    <connection>${github.url}</connection>
    <developerConnection>${github.url}</developerConnection>
    <url>${github.url}</url>
  </scm>
  <distributionManagement>
    <repository>
        <id>wrench***REMOVED***</id>
        <name>wrench***REMOVED***-releases</name>
        <url>http://seqwaremaven.oicr.on.ca/artifactory/dcc-release</url>
    </repository>
    <snapshotRepository>
        <id>wrench***REMOVED***</id>
        <name>wrench***REMOVED***-snapshots</name>
        <url>http://seqwaremaven.oicr.on.ca/artifactory/dcc-snapshot</url>
    </snapshotRepository>
  </distributionManagement>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>${guava.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.inject</groupId>
        <artifactId>guice</artifactId>
        <version>${guice.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.inject.extensions</groupId>
        <artifactId>guice-multibindings</artifactId>
        <version>${guice.version}</version>
      </dependency>
      <dependency>
        <groupId>com.typesafe</groupId>
        <artifactId>config</artifactId>
        <version>${typesafe.config.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>2.5.1</version>
          <inherited>true</inherited>
          <configuration>
            <source>1.6</source>
            <target>1.6</target>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
  <properties>
    <typesafe.config.version>0.4.0</typesafe.config.version>
    <guice.version>3.0</guice.version>
    <guava.version>12.0</guava.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <github.url>scm:git:git@github.com:icgc-dcc/data-submission.git</github.url><!-- TODO: need to corresponding children poms when more than one project -->
  </properties>
</project>
